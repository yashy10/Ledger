fastapi==0.115.0
uvicorn[standard]==0.32.0
google-generativeai==0.8.3
requests==2.32.3
python-multipart==0.0.12

# MCP gateway (ledger_gateway.py + gq_sandbox_server.py + mcp_demo_client.py)
mcp>=1.10.0
httpx>=0.27
# FastAPI 0.115.0 requires starlette<0.39; pin it so a drifted global env
# (or a transitive dep) can't pull an incompatible newer starlette.
starlette>=0.37.2,<0.39
